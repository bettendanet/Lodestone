#version 430 core

layout (local_size_x = 10, local_size_y = 10, local_size_z = 1) in;

layout(std430, binding=0) buffer shader_data {
	float Position[];
};

void main() {
	uint idx = gl_GlobalInvocationID.x + gl_GlobalInvocationID.y * 10;
	Position[idx] = Position[idx] + 0.1;
}